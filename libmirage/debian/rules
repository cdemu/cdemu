#!/usr/bin/make -f
# -*- makefile -*-


#export DH_VERBOSE=1


override_dh_auto_configure:
	dh_auto_configure -- --disable-static --enable-gtk-doc

override_dh_strip:
	dh_strip --dbg-package="libmirage6-dbg"

override_dh_makeshlibs:
	dh_makeshlibs --exclude="libmirage-1.5"

override_dh_shlibdeps:
	dh_shlibdeps --exclude="libmirage-1.5"

build:
	-test -x autogen.sh && ./autogen.sh
	dh build --with autotools_dev

clean:
	# Hack! Because GTK-Doc is retarded.
	rm -f docs/reference/libmirage/html/*.html

	dh clean --with autotools_dev

%:
	dh $@ --with autotools_dev

