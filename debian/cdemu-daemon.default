# Start daemon at system startup (system-wide)?
# Note: By default the daemon is loaded at session login
LOAD_DAEMON="0"

# Location of daemon executable
DAEMON="/usr/bin/cdemud"

# Libao audio driver
# Note: I recommended to change the value in /etc/libao.conf instead
AUDIO_DRIVER="default"

# Number of (virtual disc) devices
DEVICES="2"

# Control device
CTL_DEVICE="/dev/vhba_ctl"

